
package rsyntaxEdit

import scalaExec.Interpreter.GlobalValues

// process the word under mouse cursor position for the JSyntaxPane editor

object ProcessWordAtCursorJSyntaxPane {

def processWordAtCursorJSyntaxPane(wd: String) = {

  /* val editor = scalaExec.Interpreter.GlobalValues.editorPane
    
     val wordAtCursor = wd
      

     GlobalValues.lastVariableUnderMouseCursor = wordAtCursor
    var valueOfId = GlobalValues.scala3Engine.getBindings(0).get(wordAtCursor)
  System.out.println("wordAtCursor = "+wordAtCursor+" value = "+valueOfId)
  if (valueOfId != null)
          editor.setToolTipText(wordAtCursor+" [ "+ valueOfId+" ]  ")

}

     */
}
}